# ──────────────────────────────────────────────
# Dashy — Environment Configuration
# ──────────────────────────────────────────────
# Copy this file to `.env` and customize values.
# Variables with defaults can be omitted.
# ──────────────────────────────────────────────

# ─── General ─────────────────────────────────
NODE_ENV=development
PORT=3000
LOG_LEVEL=info

# ─── Database (SQLite) ───────────────────────
# Path to the SQLite database file (relative to project root)
DATABASE_PATH=./data/dashy.db

# ─── Auth / Sessions ─────────────────────────
# IMPORTANT: Change this to a random string of at least 32 characters in production!
SESSION_SECRET=change-me-to-a-random-secret-at-least-32-chars!!
SESSION_MAX_AGE_DAYS=7

# ─── CORS ────────────────────────────────────
# Allowed origin(s) for CORS. Use * for development, restrict in production.
CORS_ORIGIN=*

# ─── External APIs (optional) ────────────────
# OpenWeatherMap API key for the weather widget
# Get one at: https://openweathermap.org/api
# OPENWEATHER_API_KEY=your_api_key_here
